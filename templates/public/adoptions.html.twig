{% extends 'base.html.twig' %}

{% block title %}Animaux à Adopter - FindMyAsso{% endblock %}

{% block body %}
<!-- Hero Section -->
<div class="relative bg-gradient-to-br from-primary-600 via-primary-700 to-green-600 overflow-hidden">
    <div class="absolute inset-0 bg-black/10"></div>
    <div class="absolute inset-0">
        <div class="absolute inset-0 bg-gradient-to-r from-primary-600/90 to-green-600/90"></div>
        <div class="absolute inset-0" style="background-image: url('data:image/svg+xml,%3Csvg width="60" height="60" viewBox="0 0 60 60" xmlns="http://www.w3.org/2000/svg"%3E%3Cg fill="none" fill-rule="evenodd"%3E%3Cg fill="%23ffffff" fill-opacity="0.1"%3E%3Ccircle cx="30" cy="30" r="2"/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');"></div>
    </div>
    
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
        <div class="text-center">
            <div class="mx-auto h-20 w-20 bg-white/20 rounded-full flex items-center justify-center mb-6 backdrop-blur-sm">
                <i class="fas fa-paw text-white text-3xl"></i>
            </div>
            
            <h1 class="text-4xl md:text-5xl font-bold text-white mb-6">
                Trouvez votre compagnon idéal
            </h1>
            
            <p class="text-xl text-white/90 max-w-3xl mx-auto leading-relaxed">
                Des milliers d'animaux attendent une famille aimante. Découvrez des chiens, chats, lapins et bien d'autres 
                qui cherchent leur foyer pour toujours.
            </p>
        </div>
    </div>
</div>

<!-- Filters Section -->
<div class="bg-white border-b border-gray-200 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <form method="GET" class="grid grid-cols-1 md:grid-cols-5 gap-4">
            <div>
                <label for="species" class="block text-sm font-medium text-gray-700 mb-2">Espèce</label>
                <select id="species" name="species" class="w-full border border-gray-300 rounded-xl px-3 py-2 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all duration-200">
                    <option value="">Toutes les espèces</option>
                    {% for species in species %}
                        <option value="{{ species }}" {% if currentFilters.species == species %}selected{% endif %}>{{ species }}</option>
                    {% endfor %}
                </select>
            </div>
            
            <div>
                <label for="size" class="block text-sm font-medium text-gray-700 mb-2">Taille</label>
                <select id="size" name="size" class="w-full border border-gray-300 rounded-xl px-3 py-2 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all duration-200">
                    <option value="">Toutes les tailles</option>
                    <option value="small" {% if currentFilters.size == 'small' %}selected{% endif %}>Petit</option>
                    <option value="medium" {% if currentFilters.size == 'medium' %}selected{% endif %}>Moyen</option>
                    <option value="large" {% if currentFilters.size == 'large' %}selected{% endif %}>Grand</option>
                </select>
            </div>
            
            <div>
                <label for="sex" class="block text-sm font-medium text-gray-700 mb-2">Sexe</label>
                <select id="sex" name="sex" class="w-full border border-gray-300 rounded-xl px-3 py-2 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all duration-200">
                    <option value="">Tous les sexes</option>
                    <option value="male" {% if currentFilters.sex == 'male' %}selected{% endif %}>Mâle</option>
                    <option value="female" {% if currentFilters.sex == 'female' %}selected{% endif %}>Femelle</option>
                </select>
            </div>
            
            <div>
                <label for="region" class="block text-sm font-medium text-gray-700 mb-2">Région</label>
                <select id="region" name="region" class="w-full border border-gray-300 rounded-xl px-3 py-2 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all duration-200">
                    <option value="">Toutes les régions</option>
                    {% for region in regions %}
                        <option value="{{ region }}" {% if currentFilters.region == region %}selected{% endif %}>{{ region }}</option>
                    {% endfor %}
                </select>
            </div>
            
            <div class="flex items-end">
                <button type="submit" class="w-full bg-primary-600 hover:bg-primary-700 text-white px-4 py-2 rounded-xl font-semibold transition-all duration-200 transform hover:scale-105 shadow-lg">
                    <i class="fas fa-search mr-2"></i>Filtrer
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Animals Grid -->
<div class="bg-gray-50 min-h-screen py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {% if animals|length > 0 %}
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                {% for animal in animals %}
                    <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
                        <!-- Animal Image -->
                        <div class="h-64 bg-gradient-to-br from-primary-100 to-green-100 flex items-center justify-center relative overflow-hidden">
                            {% if animal.photos|length > 0 %}
                                <img src="{{ asset('uploads/animals/' ~ animal.photos[0].filename) }}"
                                     alt="{{ animal.name }}"
                                     class="w-full h-full object-cover">
                            {% else %}
                                <div class="text-center">
                                    <i class="fas fa-paw text-primary-400" style="font-size: 3rem;"></i>
                                </div>
                            {% endif %}
                            
                            <!-- Status badge -->
                            <div class="absolute top-4 left-4">
                                {% if animal.isAvailable %}
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                        <i class="fas fa-heart mr-1"></i>Disponible
                                    </span>
                                {% else %}
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-red-100 text-red-800">
                                        <i class="fas fa-times mr-1"></i>Adopté
                                    </span>
                                {% endif %}
                            </div>
                            
                            <!-- Age badge -->
                            {% if animal.age %}
                                <div class="absolute top-4 right-4">
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                        <i class="fas fa-birthday-cake mr-1"></i>{{ animal.age }} ans
                                    </span>
                                </div>
                            {% endif %}
                        </div>
                        
                        <!-- Animal Info -->
                        <div class="p-6">
                            <h3 class="text-xl font-bold text-gray-900 mb-3">{{ animal.name }}</h3>
                            
                            <!-- Basic info -->
                            <div class="flex items-center space-x-4 text-sm text-gray-600 mb-4">
                                {% if animal.species %}
                                    <span class="flex items-center">
                                        <i class="fas fa-paw mr-2 text-primary-500"></i>
                                        {{ animal.species }}
                                    </span>
                                {% endif %}
                                
                                {% if animal.breed %}
                                    <span class="flex items-center">
                                        <i class="fas fa-tag mr-2 text-secondary-500"></i>
                                        {{ animal.breed }}
                                    </span>
                                {% endif %}
                            </div>
                            
                            <!-- Size and Sex -->
                            <div class="flex items-center space-x-4 text-sm text-gray-600 mb-4">
                                {% if animal.size %}
                                    <span class="flex items-center">
                                        <i class="fas fa-ruler-combined mr-2 text-primary-500"></i>
                                        {{ animal.size == 'small' ? 'Petit' : (animal.size == 'medium' ? 'Moyen' : 'Grand') }}
                                    </span>
                                {% endif %}
                                
                                {% if animal.sex %}
                                    <span class="flex items-center">
                                        <i class="fas fa-venus-mars mr-2 text-primary-500"></i>
                                        {{ animal.sex == 'male' ? 'Mâle' : 'Femelle' }}
                                    </span>
                                {% endif %}
                            </div>
                            
                            <!-- Description -->
                            <p class="text-gray-600 mb-6" style="display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;">
                                {{ animal.description|length > 100 ? animal.description|slice(0, 100) ~ '...' : animal.description }}
                            </p>
                            
                            <!-- Association info -->
                            {% if animal.association %}
                                <div class="mb-6 p-3 bg-gray-50 rounded-lg">
                                    <div class="flex items-center">
                                        <i class="fas fa-heart text-secondary-500 mr-2"></i>
                                        <span class="text-sm font-medium text-gray-700">{{ animal.association.name }}</span>
                                    </div>
                                    <div class="text-xs text-gray-500 mt-1">{{ animal.association.city }}, {{ animal.association.region }}</div>
                                </div>
                            {% endif %}
                            
                            <!-- Actions -->
                            <div class="flex space-x-3">
                                <a href="{{ path('app_adoption_show', {'id': animal.id}) }}"
                                   class="flex-1 bg-primary-600 hover:bg-primary-700 text-white text-center py-3 px-4 rounded-xl font-medium transition-all duration-200 transform hover:scale-105 shadow-md">
                                    <i class="fas fa-eye mr-2"></i>Voir le détail
                                </a>
                                
                                {% if animal.isAvailable %}
                                    <a href="{{ path('app_adoption_request', {'animalId': animal.id}) }}"
                                       class="bg-green-600 hover:bg-green-700 text-white px-4 py-3 rounded-xl font-medium transition-colors shadow-md">
                                        <i class="fas fa-heart"></i>
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            
            <!-- Pagination -->
            {% if animals|length > 0 %}
                <div class="mt-12 flex justify-center">
                    <nav class="flex items-center space-x-2">
                        {% if page > 1 %}
                            <a href="?page={{ page - 1 }}{% for key, value in currentFilters %}{% if value %}&{{ key }}={{ value }}{% endif %}{% endfor %}"
                               class="px-4 py-2 border border-gray-300 rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors">
                                <i class="fas fa-chevron-left mr-2"></i>Précédent
                            </a>
                        {% endif %}
                        
                        <span class="px-4 py-2 text-sm text-gray-700 bg-gray-100 rounded-lg">
                            Page {{ page|default(1) }}
                        </span>
                        
                        {% if animals|length == 12 %}
                            <a href="?page={{ page + 1 }}{% for key, value in currentFilters %}{% if value %}&{{ key }}={{ value }}{% endif %}{% endfor %}"
                               class="px-4 py-2 border border-gray-300 rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors">
                                Suivant<i class="fas fa-chevron-right ml-2"></i>
                            </a>
                        {% endif %}
                    </nav>
                </div>
            {% endif %}
            
        {% else %}
            <!-- Empty State -->
            <div class="text-center py-16">
                <div class="mx-auto h-24 w-24 bg-gray-100 rounded-full flex items-center justify-center mb-6">
                    <i class="fas fa-search text-gray-400 text-3xl"></i>
                </div>
                
                <h3 class="text-xl font-medium text-gray-900 mb-4">Aucun animal trouvé</h3>
                
                <p class="text-gray-600 mb-8 max-w-md mx-auto">
                    Aucun animal ne correspond à vos critères de recherche. 
                    Essayez de modifier vos filtres ou revenez plus tard.
                </p>
                
                <a href="{{ path('app_adoptions') }}" 
                   class="inline-flex items-center px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-medium rounded-xl transition-colors">
                    <i class="fas fa-refresh mr-2"></i>
                    Voir tous les animaux
                </a>
            </div>
        {% endif %}
    </div>
</div>

<!-- CTA Section -->
<div class="bg-gradient-to-r from-primary-600 to-green-600 py-20">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
            Prêt à adopter ?
        </h2>
        
        <p class="text-xl text-white/90 mb-8">
            L'adoption est un acte d'amour qui change deux vies : celle de l'animal et la vôtre. 
            Découvrez comment procéder et préparez-vous à accueillir votre nouveau compagnon.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="{{ path('app_about') }}" 
               class="inline-flex items-center justify-center px-8 py-4 bg-white text-primary-700 font-semibold rounded-full text-lg shadow-lg hover:bg-gray-100 transition-all duration-300 transform hover:scale-105">
                <i class="fas fa-info-circle mr-3"></i>
                Guide de l'adoption
            </a>
            
            <a href="{{ path('app_contact') }}" 
               class="inline-flex items-center justify-center px-8 py-4 bg-transparent border-2 border-white text-white font-semibold rounded-full text-lg hover:bg-white hover:text-primary-700 transition-all duration-300 transform hover:scale-105">
                <i class="fas fa-envelope mr-3"></i>
                Nous contacter
            </a>
        </div>
    </div>
</div>

<!-- Adoption Tips -->
<div class="bg-white py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
                Conseils pour une adoption réussie
            </h2>
            <p class="text-xl text-gray-600 max-w-2xl mx-auto">
                Quelques points importants à considérer avant d'adopter un animal
            </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div class="text-center">
                <div class="mx-auto h-16 w-16 bg-primary-100 rounded-full flex items-center justify-center mb-6">
                    <i class="fas fa-home text-primary-600 text-2xl"></i>
                </div>
                <h3 class="text-xl font-semibold text-gray-900 mb-4">Préparer votre foyer</h3>
                <p class="text-gray-600">
                    Assurez-vous que votre logement est adapté et sécurisé pour accueillir votre nouvel animal.
                </p>
            </div>
            
            <div class="text-center">
                <div class="mx-auto h-16 w-16 bg-secondary-100 rounded-full flex items-center justify-center mb-6">
                    <i class="fas fa-clock text-secondary-600 text-2xl"></i>
                </div>
                <h3 class="text-xl font-semibold text-gray-900 mb-4">Prendre le temps</h3>
                <p class="text-gray-600">
                    L'adoption est un engagement à long terme. Prenez le temps de bien réfléchir et de vous préparer.
                </p>
            </div>
            
            <div class="text-center">
                <div class="mx-auto h-16 w-16 bg-green-100 rounded-full flex items-center justify-center mb-6">
                    <i class="fas fa-heart text-green-600 text-2xl"></i>
                </div>
                <h3 class="text-xl font-semibold text-gray-900 mb-4">Être patient</h3>
                <p class="text-gray-600">
                    Chaque animal a son caractère. Soyez patient et donnez-lui le temps de s'adapter à son nouveau foyer.
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}
