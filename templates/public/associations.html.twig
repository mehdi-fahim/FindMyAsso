{% extends 'base.html.twig' %}

{% block title %}Associations de Protection Animale - FindMyAsso{% endblock %}

{% block body %}
<!-- Hero Section -->
<div class="relative bg-gradient-to-br from-secondary-600 via-secondary-700 to-primary-600 overflow-hidden">
    <div class="absolute inset-0 bg-black/10"></div>
    <div class="absolute inset-0">
        <div class="absolute inset-0 bg-gradient-to-r from-secondary-600/90 to-primary-600/90"></div>
        <div class="absolute inset-0" style="background-image: url('data:image/svg+xml,%3Csvg width="60" height="60" viewBox="0 0 60 60" xmlns="http://www.w3.org/2000/svg"%3E%3Cg fill="none" fill-rule="evenodd"%3E%3Cg fill="%23ffffff" fill-opacity="0.1"%3E%3Ccircle cx="30" cy="30" r="2"/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');"></div>
    </div>
    
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
        <div class="text-center">
            <div class="mx-auto h-20 w-20 bg-white/20 rounded-full flex items-center justify-center mb-6 backdrop-blur-sm">
                <i class="fas fa-heart text-white text-3xl"></i>
            </div>
            
            <h1 class="text-4xl md:text-5xl font-bold text-white mb-6">
                Associations de Protection Animale
            </h1>
            
            <p class="text-xl text-white/90 max-w-3xl mx-auto leading-relaxed">
                Découvrez des associations engagées qui œuvrent chaque jour pour sauver et protéger les animaux en détresse.
            </p>
        </div>
    </div>
</div>

<!-- Filters Section -->
<div class="bg-white border-b border-gray-200 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <form method="GET" class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <div>
                <label for="region" class="block text-sm font-medium text-gray-700 mb-3">Région</label>
                <select id="region" name="region" class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all duration-200">
                    <option value="">Toutes les régions</option>
                    {% for region in regions %}
                        <option value="{{ region }}" {% if currentRegion == region %}selected{% endif %}>{{ region }}</option>
                    {% endfor %}
                </select>
            </div>
            
            <div>
                <label for="species" class="block text-sm font-medium text-gray-700 mb-3">Espèce</label>
                <select id="species" name="species" class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all duration-200">
                    <option value="">Toutes les espèces</option>
                    {% for species in species %}
                        <option value="{{ species }}" {% if currentSpecies == species %}selected{% endif %}>{{ species }}</option>
                    {% endfor %}
                </select>
            </div>
            
            <div class="flex items-end">
                <button type="submit" class="w-full bg-primary-600 hover:bg-primary-700 text-white px-6 py-3 rounded-xl font-semibold transition-all duration-200 transform hover:scale-105 shadow-lg">
                    <i class="fas fa-search mr-2"></i>Filtrer
                </button>
            </div>
        </form>
    </div>
</div>

<!-- Associations Grid -->
<div class="bg-gray-50 min-h-screen py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {% if associations|length > 0 %}
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                {% for association in associations %}
                    <div class="bg-white rounded-2xl shadow-lg overflow-hidden hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2">
                        <!-- Header with image -->
                        <div class="h-48 bg-gradient-to-br from-primary-100 to-secondary-100 flex items-center justify-center relative overflow-hidden">
                            {% if association.logo %}
                                <img src="{{ asset('uploads/associations/' ~ association.logo) }}"
                                     alt="{{ association.name }}"
                                     class="w-full h-full object-cover">
                            {% else %}
                                <div class="text-center">
                                    <i class="fas fa-heart text-primary-400 text-5xl mb-2"></i>
                                    <p class="text-primary-600 font-medium">{{ association.name }}</p>
                                </div>
                            {% endif %}
                            
                            <!-- Status badge -->
                            <div class="absolute top-4 right-4">
                                {% if association.isApproved %}
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                        <i class="fas fa-check-circle mr-1"></i>Approuvée
                                    </span>
                                {% else %}
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                                        <i class="fas fa-clock mr-1"></i>En attente
                                    </span>
                                {% endif %}
                            </div>
                        </div>
                        
                        <!-- Content -->
                        <div class="p-6">
                            <h3 class="text-xl font-bold text-gray-900 mb-3">
                                <a href="{{ path('app_association_show', {'id': association.id}) }}" class="hover:text-primary-600 transition-colors">
                                    {{ association.name }}
                                </a>
                            </h3>
                            
                            <div class="flex items-center text-gray-500 mb-4">
                                <i class="fas fa-map-marker-alt mr-2 text-primary-500"></i>
                                <span>{{ association.city }}, {{ association.region }}</span>
                            </div>
                            
                            <p class="text-gray-600 mb-6" style="display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden;">
                                {{ association.description|length > 150 ? association.description|slice(0, 150) ~ '...' : association.description }}
                            </p>
                            
                            <!-- Capacity info -->
                            {% if association.shelterCapacities|length > 0 %}
                                <div class="mb-4">
                                    <h4 class="text-sm font-medium text-gray-700 mb-2">Capacités d'accueil :</h4>
                                    <div class="flex flex-wrap gap-2">
                                        {% for capacity in association.shelterCapacities|slice(0, 3) %}
                                            <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium bg-primary-100 text-primary-800">
                                                {{ capacity.species }}: {{ capacity.capacityAvailable }}/{{ capacity.capacityTotal }}
                                            </span>
                                        {% endfor %}
                                        {% if association.shelterCapacities|length > 3 %}
                                            <span class="inline-flex items-center px-2.5 py-1 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                                                +{{ association.shelterCapacities|length - 3 }} autres
                                            </span>
                                        {% endif %}
                                    </div>
                                </div>
                            {% endif %}
                            
                            <!-- Stats -->
                            <div class="grid grid-cols-2 gap-4 mb-6">
                                <div class="text-center p-3 bg-gray-50 rounded-lg">
                                    <div class="text-2xl font-bold text-primary-600">
                                        {% set totalAnimals = 0 %}
                                        {% for animal in association.animals %}
                                            {% if animal.isAvailable %}
                                                {% set totalAnimals = totalAnimals + 1 %}
                                            {% endif %}
                                        {% endfor %}
                                        {{ totalAnimals }}
                                    </div>
                                    <div class="text-sm text-gray-600">Animaux disponibles</div>
                                </div>
                                
                                <div class="text-center p-3 bg-gray-50 rounded-lg">
                                    <div class="text-2xl font-bold text-secondary-600">
                                        {% set totalRequests = 0 %}
                                        {% for animal in association.animals %}
                                            {% if animal.adoptionRequests is defined %}
                                                {% set totalRequests = totalRequests + animal.adoptionRequests|length %}
                                            {% endif %}
                                        {% endfor %}
                                        {{ totalRequests }}
                                    </div>
                                    <div class="text-sm text-gray-600">Demandes d'adoption</div>
                                </div>
                            </div>
                            
                            <!-- Actions -->
                            <div class="flex space-x-3">
                                <a href="{{ path('app_association_show', {'id': association.id}) }}"
                                   class="flex-1 bg-primary-600 hover:bg-primary-700 text-white text-center py-3 px-4 rounded-xl font-medium transition-all duration-200 transform hover:scale-105 shadow-md">
                                    <i class="fas fa-eye mr-2"></i>Voir le détail
                                </a>
                                
                                {% if association.website %}
                                    <a href="{{ association.website }}" target="_blank" rel="noopener noreferrer"
                                       class="bg-gray-100 hover:bg-gray-200 text-gray-700 px-4 py-3 rounded-xl font-medium transition-colors">
                                        <i class="fas fa-external-link-alt"></i>
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            
            <!-- Pagination -->
            {% if associations|length > 0 %}
                <div class="mt-12 flex justify-center">
                    <nav class="flex items-center space-x-2">
                        {% if page > 1 %}
                            <a href="?page={{ page - 1 }}{% if currentRegion %}&region={{ currentRegion }}{% endif %}{% if currentSpecies %}&species={{ currentSpecies }}{% endif %}"
                               class="px-4 py-2 border border-gray-300 rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors">
                                <i class="fas fa-chevron-left mr-2"></i>Précédent
                            </a>
                        {% endif %}
                        
                        <span class="px-4 py-2 text-sm text-gray-700 bg-gray-100 rounded-lg">
                            Page {{ page }}
                        </span>
                        
                        {% if associations|length == 12 %}
                            <a href="?page={{ page + 1 }}{% if currentRegion %}&region={{ currentRegion }}{% endif %}{% if currentSpecies %}&species={{ currentSpecies }}{% endif %}"
                               class="px-4 py-2 border border-gray-300 rounded-lg text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors">
                                Suivant<i class="fas fa-chevron-right ml-2"></i>
                            </a>
                        {% endif %}
                    </nav>
                </div>
            {% endif %}
            
        {% else %}
            <!-- Empty State -->
            <div class="text-center py-16">
                <div class="mx-auto h-24 w-24 bg-gray-100 rounded-full flex items-center justify-center mb-6">
                    <i class="fas fa-search text-gray-400 text-3xl"></i>
                </div>
                
                <h3 class="text-xl font-medium text-gray-900 mb-4">Aucune association trouvée</h3>
                
                <p class="text-gray-600 mb-8 max-w-md mx-auto">
                    Aucune association ne correspond à vos critères de recherche. 
                    Essayez de modifier vos filtres ou revenez plus tard.
                </p>
                
                <a href="{{ path('app_associations') }}" 
                   class="inline-flex items-center px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-medium rounded-xl transition-colors">
                    <i class="fas fa-refresh mr-2"></i>
                    Voir toutes les associations
                </a>
            </div>
        {% endif %}
    </div>
</div>

<!-- CTA Section -->
<div class="bg-gradient-to-r from-primary-600 to-secondary-600 py-20">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
            Vous êtes une association ?
        </h2>
        
        <p class="text-xl text-white/90 mb-8">
            Rejoignez notre réseau et bénéficiez de notre plateforme pour trouver des familles d'accueil, 
            des adoptants et des donateurs pour vos animaux.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="{{ path('app_contact') }}" 
               class="inline-flex items-center justify-center px-8 py-4 bg-white text-primary-700 font-semibold rounded-full text-lg shadow-lg hover:bg-gray-100 transition-all duration-300 transform hover:scale-105">
                <i class="fas fa-handshake mr-3"></i>
                Nous rejoindre
            </a>
            
            <a href="{{ path('app_about') }}" 
               class="inline-flex items-center justify-center px-8 py-4 bg-transparent border-2 border-white text-white font-semibold rounded-full text-lg hover:bg-white hover:text-primary-700 transition-all duration-300 transform hover:scale-105">
                <i class="fas fa-info-circle mr-3"></i>
                En savoir plus
            </a>
        </div>
    </div>
</div>
{% endblock %}
